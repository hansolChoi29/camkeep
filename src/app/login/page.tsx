import React from "react";

export default function LoginPage() {
  return (
    <main className="min-h-screen flex font-sans">
      {/* 이 안의 텍스트는 system‑ui, sans‑serif → 글로벌 폰트 덮어쓰기 */}
      <form className="w-full max-w-sm">dsd</form>
    </main>
  );
}
